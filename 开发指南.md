# map.ruiduobao.com 开发指南

> 更新时间: 2026-05-27
> 文档版本: 3.12

---

## 一、项目概述

### 1.1 项目简介

**map.ruiduobao.com** 是一个中国五级行政区划查询与下载系统，提供：

- **省级/市级/县级/乡镇级/村级** 五级行政区划数据查询
- **多年份数据**: 2010、2017、2018、2021、2023 年行政区划数据（支持年份切换）
- **矢量数据导出**: GeoJSON、Shapefile、SVG、KML、GeoPackage 格式
- **地理编码**: 地名转坐标功能
- **多底图支持**: ESRI 卫星、必应卫星、星图地球、高德卫星等
- **村级点位数据**: 60万+村级点位数据
- **行政区划搜索**: 支持按名称或编码搜索
- **数据可视化**: 统计图表 API
- **自定义边界**: 支持用户上传 GeoJSON 边界
- **移动端适配**: 专门的手机端下拉选择器，响应式设计

> ⚠️ **注意**: 村级面数据目前默认禁用，系统只提供村级点位数据。如需启用面数据，请联系管理员修改配置 `village.enablePolygon = true`。

### 1.2 技术栈

| 层级 | 技术 |
|------|------|
| 后端框架 | Express.js (Node.js) |
| 数据库 | PostgreSQL + PostGIS |
| 前端地图 | 高德地图 JS API |
| 模板引擎 | EJS |
| 数据可视化 | D3.js |

### 1.3 项目结构

```
map.ruiduobao.com/
├── app.js                    # 主应用入口
├── package.json              # 依赖配置
├── views/                    # EJS 模板
│   └── index.ejs             # 主页面模板
├── public/                   # 静态资源
│   ├── js/
│   │   └── main.js           # 前端 JavaScript
│   ├── css/
│   │   └── styles.css        # 样式文件（含响应式）
│   ├── vectordata/           # 生成的矢量数据缓存
│   ├── others/               # 静态 HTML 文件
│   ├── pics/                 # 图片资源
│   ├── mobile-selector.html  # 手机端下拉选择器 (v3.1新增)
│   ├── province-selector.html # PC端省份选择器
│   └── tree.html             # 树状视图页面
├── year/                     # 历史行政区划 HTML
│   ├── 2010年/
│   ├── 2017年/
│   ├── 2018年/
│   ├── 2021年/
│   └── 2023年/
├── 处理脚本/                 # 数据处理脚本
└── 省市县乡村的编码markdown/  # 行政区划编码数据
```

---

## 二、数据库配置

### 2.1 数据库连接

**配置信息** (位于 `app.js`):

```javascript
const dbConfig = {
    host: 'localhost',
    port: 5432,
    database: 'shengshixian',
    user: 'ruiduobao',
    password: 'RDB123456.'
};
```

### 2.2 数据库架构

**数据库名**: `shengshixian`

| Schema | 表名 | 说明 |
|--------|------|------|
| `SHENG` | `CHN_sheng_{year}` | 省级边界 (2010, 2017, 2018, 2021, 2023) |
| `SHI` | `CHN_shi_{year}` | 市级边界 (2010, 2013-2023) |
| `XIAN` | `CHN_xian_{year}` | 县级边界 (2010, 2013-2023) |
| `XIANG` | `CHN_xiang_2020` | 乡镇级边界 (43,206条，编码为12位) |
| `CUN` | `chinacunpoint` | 村级点位 (619,245条，cuncode字段存储县级编码)|
| `cunpolygon` | `{城市代码}` | 村级面数据（140个城市，**当前禁用**）|

> ⚠️ **村级面数据说明**: 由于数据量较大，村级面数据默认禁用。当前系统只使用村级点位数据（`CUN.chinacunpoint`）。

### 2.3 行政区划编码规则（重要）

| 代码格式 | 级别 | 示例 | 说明 |
|----------|------|------|------|
| `XX0000` | 省级 | `510000` | 前2位+0000 |
| `XXXX00` | 地级 | `510100` | 前4位+00 |
| `XXXXXX` | 县级 | `510104` | 6位数字 |
| `XXXXXXXXX000` | 乡镇级 | `510104021000` | 9位+000，共12位 |
| `XXXXXXXXXXXX` | 村级 | `140105001999` | 12位数字 |

**编码查询逻辑**:
1. 省级(6位后4位为0000) → 查询 `SHENG.CHN_sheng_{year}`
2. 地级(6位后2位为00) → 查询 `SHI.CHN_shi_{year}`
3. 县级(6位后2位不为00) → 查询 `XIAN.CHN_xian_{year}`
4. 乡镇级(12位以000结尾) → 查询 `XIANG.CHN_xiang_2020`
5. 村级(12位不以000结尾) → 查询 `cunpolygon.{城市代码}` 的 `cun_code` 字段

### 2.4 村级数据说明

**重要**: 村级数据存储在两个表中，字段含义不同：

| 表 | 字段 | 存储内容 | 用途 |
|-----|------|----------|------|
| `cunpolygon.{市级编码}` | `cun_code` | 真正的12位村级编码 | 用于村级面数据匹配 |
| `CUN.chinacunpoint` | `cuncode` | 县级编码(6位) | 用于地理位置参考 |

**面数据表命名规则（重要）**:
- 表名格式: `cunpolygon.{编码}`，编码可能是：
  1. **市级编码(6位)**: 如 `140100`（太原市）
  2. **省级编码(6位)**: 如 `510000`（四川省）
  3. **省份名称**: 如 `HENAN`（河南省）、`HUBEI`（湖北省）

**查询逻辑**: 依次尝试市级编码、省级编码、省份名称查找表

**面数据编码后缀含义**:
| 后缀 | 含义 | 示例 |
|------|------|------|
| `999` | 街道办/乡镇整体边界 | `140105001999` (坞城街道办事处) |
| `200-299` | 行政村边界 | `140105003205` (黑驼村) |
| 其他 | 其他类型村/社区 | - |

**村级编码查询流程**:
```
用户输入村级编码(如140105001001)
    ↓
提取市级编码(前4位+'00'): 140100
    ↓
查询 cunpolygon.140100 表
    ↓
1. cun_code 精确匹配 → 成功返回
2. 乡镇前缀匹配（优先非999）→ 返回行政村
3. 乡镇前缀匹配（含999）→ 返回街道办整体
4. 县级前缀匹配 → 返回任意有边界的村
```

**数据统计 (2026-03-13)**:

| 年份 | 省级 | 地级 | 县级 | 乡镇级 | 村级点位 | 村级面数据城市 |
|------|------|------|------|--------|----------|---------------|
| 2010 | 34 | 367 | 2868 | - | - | - |
| 2017 | 34 | 370 | 2862 | - | - | - |
| 2018 | 34 | 369 | 2861 | - | - | - |
| 2021 | 34 | 370 | 2852 | - | - | - |
| 2023 | 34 | 372 | 2853 | - | - | - |
| 2020 | - | - | - | 43,206 | 619,245 | 140 |

---

## 三、API 接口文档

### 3.1 数据查询接口

#### 获取矢量数据 (通过行政区划代码) - 支持年份参数

```
GET /getGsonDB?code={dataCode}&year={year}
```

**参数说明**:
- `code`: 行政区划代码，支持以下格式：
  - 省级: 6位，后4位为0000（如 `510000`）
  - 地级: 6位，后2位为00（如 `510100`）
  - 县级: 6位，后2位不为00（如 `510104`）
  - 乡镇级: 12位，以000结尾（如 `510104021000`）
  - 村级: 12位，不以000结尾（如 `140105001999`）
- `year`: 年份 (可选，默认2023，支持 2010/2017/2018/2021/2023)

**响应示例**:
```json
{
    "status": "success",
    "message": "Data exported successfully",
    "filepath": "/vectordata/510000.gson"
}
```

#### 获取村级数据 (支持编码或地名)

```
GET /getCunAddress?code={cunCode}
GET /getCunAddress?address={placeName}
```

**参数说明**:
- `code`: 村级编码 (12位，优先使用)
- `address`: 地名 (如 "四川省成都市锦江区XXX村")

**响应示例**:
```json
{
    "status": "success",
    "message": "Polygon",
    "filepath": "/vectordata/XXX村.gson"
}
```

#### 行政区划搜索 (新增)

```
GET /search?keyword={keyword}&level={level}&year={year}
```

**参数说明**:
- `keyword`: 搜索关键词
- `level`: 级别过滤 (可选: sheng/shi/xian/cun/all，默认 all)
- `year`: 年份 (可选，默认2023)
- `limit`: 返回数量限制 (可选，默认20)

**响应示例**:
```json
{
    "status": "success",
    "data": {
        "sheng": [{"name": "四川省", "code": "510000", "level": "sheng"}],
        "shi": [{"name": "成都市", "code": "510100", "level": "shi"}],
        "xian": [],
        "cun": []
    }
}
```

#### 统计数据 API (新增)

```
GET /api/statistics?year={year}
```

**响应示例**:
```json
{
    "status": "success",
    "data": {
        "year": 2023,
        "provinces": 34,
        "cities": 372,
        "counties": 2853,
        "villages": 619245
    }
}
```

#### 历史变化 API (新增)

```
GET /api/history?code={code}
```

**响应示例**:
```json
{
    "status": "success",
    "data": [
        {"year": 2023, "exists": true, "data": {"name": "成都市", "code": "510100"}},
        {"year": 2021, "exists": true, "data": {"name": "成都市", "code": "510100"}},
        {"year": 2018, "exists": true, "data": {"name": "成都市", "code": "510100"}},
        {"year": 2017, "exists": true, "data": {"name": "成都市", "code": "510100"}},
        {"year": 2010, "exists": true, "data": {"name": "成都市", "code": "510100"}}
    ]
}
```

#### 地理编码

```
GET /getGeoAddress?address={placeName}
```

**参数说明**:
- `address`: 地名

**响应示例**:
```json
{
    "status": "success",
    "message": "Data exported successfully",
    "filepath": "/vectordata/地名.gson"
}
```

### 3.2 年份/省份接口

#### 获取年份列表

```
GET /get-years
```

**响应示例**:
```json
["2023年", "2021年", "2018年", "2017年", "2010年"]
```

#### 获取某年份的省份列表

```
GET /get-provinces/{year}
```

**响应示例**:
```json
["北京市.html", "天津市.html", "河北省.html", ...]
```

### 3.3 下载接口 - 支持多种格式

```
GET /downloadVector/{id}?format={format}&year={year}
```

**参数说明**:
- `id`: 行政区划代码（推荐）或地名
- `format`: 下载格式
  - `shp` - Shapefile (ZIP)
  - `gson` - GeoJSON
  - `svg` - SVG 矢量图
  - `kml` - KML 格式
  - `gpkg` - GeoPackage 格式
- `year`: 年份参数（可选，默认2023，支持省/市/县级多年份数据）

**重要**: 下载时建议使用行政区划代码而非地名，避免地名匹配失败。

**下载限制**: 每个 IP 最多下载 200,000,000 次

**ZIP包内容** (所有格式都打包为ZIP):
```
{id}.zip
├── {id}.geojson/.shp等  # 数据文件
├── 关注公众号_锐多宝.jpg   # 公众号二维码
├── 站长微信.png            # 站长微信二维码（反馈问题用）
└── 说明.txt               # 下载说明（含免责声明）
```

**说明.txt 内容**:
```
数据来源：map.ruiduobao.com
行政区划名称：{regionName}
行政区划编码：{id}
下载时间：{时间}

【数据内容】
- 县级批量下载：县级边界、乡镇边界、村级点数据
- 地级市批量下载：市级边界、县级边界、乡镇边界、村级点数据
- 乡镇级批量下载：乡镇边界、村级点数据

> ⚠️ **注意**: 村级面数据默认禁用，批量下载不包含村级面数据。

【数据时间属性说明】
1. 省级、市级、县级数据有时间属性（来源于CTAMap，网址：www.shengshixian.com（谐音"省市县"））
2. 乡镇、村级数据默认不变（约2020年）

【使用声明】
1. 规范使用：请遵守国家相关法律法规，规范使用本数据
2. 自己负责：使用者对数据的使用行为及其后果自行负责
3. 仅供参考：本数据仅供学术研究、教育学习等参考用途
4. 禁止商用：严禁将本数据用于任何商业用途
5. 下载默认同意：下载数据即表示您已阅读并同意以上条款

如有问题或建议，请联系邮箱：caaschengrui@163.com

关注微信公众号"锐多宝"获取更多数据
```

**批量下载** (县级):
```
GET /downloadCountyBatch/county/{countyCode}?format={format}&year=2023
```

**批量下载** (地级市):
```
GET /downloadCityBatch/city/{cityCode}?format={format}&year=2023
```

**批量下载** (乡镇级):
```
GET /downloadTownBatch/town/{townCode}?format={format}&year=2023
```

- 支持格式：`shp`、`geojson`、`gpkg`、`kml`（在界面上可选）
- 支持年份参数

**县级批量下载数据内容**:
- 县级边界数据（1个县）
- 乡镇边界数据（合并为一个文件，多个面要素）
- 村级点数据（从 chinacunpoint 表查询，按县级编码前缀匹配）

> ⚠️ 村级面数据默认禁用，不包含在批量下载中。

**地级市批量下载数据内容**:
- 市级边界数据（1个市）
- 县级边界数据（合并为一个文件，多个面要素）
- 乡镇边界数据（合并为一个文件，多个面要素）
- 村级点数据（按市级编码前缀匹配）

> ⚠️ 村级面数据默认禁用，不包含在批量下载中。

**ZIP包目录结构** (县级示例):
```
义乌市_330782_批量下载.zip
├── 县级/
│   ├── 义乌市_330782.geojson
│   └── 义乌市_330782.shp (及其他Shapefile文件)
├── 乡镇/
│   ├── 乡镇边界_330782.geojson
│   └── 乡镇边界_330782.shp
├── 村级/
│   ├── 村位置_点数据_330782.geojson
│   └── 村位置_点数据_330782.shp
├── 说明.txt
└── 关注公众号_锐多宝.jpg
└── 站长微信.png
```

> ⚠️ 村级面数据默认禁用，ZIP包中不包含 `村边界_面数据` 文件。

### 3.4 用户上传边界 (新增)

```
POST /api/upload-boundary
Content-Type: application/json

{
    "name": "自定义边界名称",
    "geojson": { ... },
    "description": "描述信息"
}
```

**响应示例**:
```json
{
    "status": "success",
    "message": "边界上传成功",
    "data": {
        "id": "abc123def456",
        "name": "自定义边界名称",
        "filepath": "/vectordata/custom_abc123def456.gson"
    }
}
```

### 3.5 静态矢量文件

```
GET /getGsonFile?code={dataCode}
```

### 3.6 树状数据API (v2.0新增)

#### 获取省份列表
```
GET /api/tree/provinces?year=2023
```

#### 获取某省的市列表
```
GET /api/tree/cities?year=2023&province=四川省
```

#### 获取某市的县列表
```
GET /api/tree/counties?year=2023&province=四川省&city=成都市
```

#### 获取某县的乡镇列表
```
GET /api/tree/towns?year=2023&province=四川省&city=成都市&county=锦江区
```

#### 获取某乡镇的村列表
```
GET /api/tree/villages?year=2023&province=四川省&city=成都市&county=锦江区&town=春熙路街道
```

#### 获取完整树状数据（推荐）
```
GET /api/tree/full?year=2023&province=四川省
```

**响应示例**:
```json
{
    "status": "success",
    "province": "四川省",
    "year": "2023",
    "data": [
        {
            "name": "成都市",
            "code": "510100",
            "counties": [
                {
                    "name": "锦江区",
                    "code": "510104",
                    "towns": [
                        {
                            "name": "春熙路街道",
                            "code": "510104017000",
                            "villages": [
                                {"name": "盐市口社区居委会", "code": "510104017001"}
                            ]
                        }
                    ]
                }
            ]
        }
    ]
}
```

### 3.7 点击查询县级API (v3.11新增)

#### 通过坐标查询县级行政区划
```
GET /getGsonDB/point-query?lng={经度}&lat={纬度}&year={年份}
```

**参数说明**:
- `lng`: 经度（必填）
- `lat`: 纬度（必填）
- `year`: 年份（可选，默认2023）

**响应示例**:
```json
{
    "status": "success",
    "data": {
        "code": "110101",
        "name": "东城区",
        "year": 2023,
        "shengCode": "110000",
        "shiCode": "110100",
        "filepath": "/vectordata/110101.gson"
    }
}
```

**技术实现**: 使用 PostGIS 空间查询 `ST_Contains(geom, ST_SetSRID(ST_MakePoint(lng, lat), 4326))` 查找包含该点的县级边界。

---

## 四、前端开发指南

### 4.1 地图初始化

```javascript
const map = new AMap.Map('mapContainer', {
    zoom: 2,
    center: [108, 35],  // 中国中心
    layers: [ESRILAYER]  // 默认 ESRI 卫星图层
});
```

### 4.2 底图图层配置

| 图层名称 | 数据源 |
|----------|--------|
| `ESRILAYER` | ESRI World Imagery (默认) |
| `BINGLAYER` | 必应卫星图 |
| `XINGTU` | 星图地球 |
| `satelliteAndRoadNetLayer` | 高德卫星+路网 |

**图层切换实现**:

```javascript
function switchMapLayer() {
    const selectedLayer = document.getElementById('mapLayer').value;
    if (selectedLayer === 'ESRI') {
        map.setLayers([ESRILAYER]);
    } else if (selectedLayer === 'bing') {
        map.setLayers([BINGLAYER]);
    } else if (selectedLayer === 'star_map') {
        map.setLayers([XINGTU]);
    } else if (selectedLayer === 'satellite_road') {
        map.setLayers(satelliteAndRoadNetLayer);
    }
}
```

**必应地图配置** (使用 quadkey 方式):

```javascript
let BINGLAYER = new AMap.TileLayer({
    getTileUrl: function(x, y, z) {
        let quadKey = '';
        for (let i = z; i > 0; i--) {
            let digit = 0;
            let mask = 1 << (i - 1);
            if ((x & mask) !== 0) digit++;
            if ((y & mask) !== 0) digit += 2;
            quadKey += digit;
        }
        return 'https://ecn.t3.tiles.virtualearth.net/tiles/a' + quadKey + '.jpeg?g=1';
    },
    tileSize: 256,
    zIndex: 100
});
```

### 4.3 消息通信机制

前端使用 `postMessage` 在主页面和 iframe 之间通信：

```javascript
// 监听 iframe 消息
window.addEventListener('message', async (event) => {
    switch(event.data.type) {
        case 'updateMapByCode':
            // 通过行政区划代码更新地图
            dataCode = event.data.code;
            
            // 根据编码长度判断级别
            if (dataCode.length === 12 && !dataCode.endsWith('000')) {
                // 村级编码
                loadCunData(dataCode);
            } else if (dataCode.length === 12 && dataCode.endsWith('000')) {
                // 乡镇级编码
                exportgsontoDIR(dataCode);
            } else {
                // 省市县级
                exportgsontoDIR(dataCode);
            }
            break;
        case 'updateMapByAddress':
            // 通过地名更新地图
            address = event.data.address;
            Codegson(address);
            break;
        case 'updateMapByYearsheng':
            // 通过年份+省份更新地图
            // 加载树状视图
            iframe.src = `/tree.html?year=${yearNum}&province=${encodeURIComponent(Province)}`;
            break;
    }
});
```

### 4.4 树状视图 (v2.1新增)

**页面路径**: `/public/tree.html`

**URL参数**:
- `year`: 年份（如 2023）
- `province`: 省份名称（如 四川省）

**功能特性**:
- **整行可点击**: 用户点击任意位置都能展开/选中节点
- **hover阴影**: 鼠标悬停显示浅蓝色背景
- **选中状态**: 点击后保持深蓝色背景，便于下载
- **搜索功能**: 先搜本省，搜不到自动搜索全国
- **编码显示**: 每个节点右侧显示编码

**数据加载流程**:
```
用户选择年份+省份
    ↓
iframe.src = /tree.html?year=2023&province=四川省
    ↓
调用 /api/tree/full?year=2023&province=四川省
    ↓
返回省→市→县→乡镇→村的完整树状数据
    ↓
用户点击节点 → postMessage → 父页面加载矢量
```

**树状视图数据结构**:
```json
{
    "status": "success",
    "province": "四川省",
    "year": "2023",
    "data": [
        {
            "name": "成都市",
            "code": "510100",
            "counties": [
                {
                    "name": "锦江区",
                    "code": "510104",
                    "towns": [
                        {
                            "name": "春熙路街道",
                            "code": "510104017000",
                            "villages": [
                                {"name": "盐市口社区居委会", "code": "510104017001"}
                            ]
                        }
                    ]
                }
            ]
        }
    ]
}
```
```

### 4.4 GeoJSON 加载

**面矢量加载**:
```javascript
function loadGeoJSONfromPath(filepath) {
    map.clearMap();
    fetch(filepath)
        .then(response => response.json())
        .then(geojsonData => {
            const geojson = new AMap.GeoJSON({
                geoJSON: geojsonData,
                getPolygon: function(geojson, lnglats) {
                    return new AMap.Polygon({
                        path: lnglats,
                        strokeColor: '#ff33cc',
                        fillColor: '#ffc3a0',
                        fillOpacity: 0.5
                    });
                }
            });
            geojson.setMap(map);
            map.setFitView();
        });
}
```

**点矢量加载**:
```javascript
function loadGeoPointJSONfromPath(filepath) {
    map.clearMap();
    fetch(filepath)
        .then(response => response.json())
        .then(geojsonData => {
            const geojson = new AMap.GeoJSON({
                geoJSON: geojsonData,
                getMarker: function(geojson, lnglats) {
                    return new AMap.Marker({
                        position: lnglats,
                        icon: new AMap.Icon({
                            image: "https://webapi.amap.com/theme/v1.3/markers/n/mark_r.png"
                        })
                    });
                }
            });
            geojson.setMap(map);
            map.setFitView();
        });
}
```

### 4.5 左侧县市列表实现原理

**架构说明**:

左侧县市列表通过 **iframe + markmap** 实现：

1. **iframe 加载**: 主页面通过 iframe 加载 `/year/{年份}/{省份}.html`
2. **markmap 渲染**: HTML 文件使用 [markmap](https://markmap.js.org/) 库将 Markdown 思维导图渲染为交互式树状图
3. **消息通信**: 用户点击树节点时，iframe 通过 `postMessage` 向父页面发送消息

**数据流**:

```
用户选择年份+省份
    ↓
iframe.src = /year/2023年/山西省.html
    ↓
markmap 渲染省市县乡村树状图
    ↓
用户点击节点 → postMessage → 父页面接收
    ↓
父页面调用 /getGsonDB?code=140000 加载矢量
```

**省份 HTML 文件结构**:

```html
<!DOCTYPE html>
<html>
<head>
    <script src="markmap相关库"></script>
</head>
<body>
    <!-- markmap 数据，包含省市县乡村的树状结构 -->
    <script>
        // 点击事件处理
        document.body.addEventListener('click', function(event) {
            const target = event.target.closest('a');
            if (target.hasAttribute('data-code')) {
                const dataCode = target.getAttribute('data-code');
                window.parent.postMessage({
                    "type": "updateMapByCode",
                    "code": dataCode
                }, '*');
            }
        });
    </script>
</body>
</html>
```

**数据生成流程** (参见第八章): CSV → Markdown → HTML (markmap)

---

## 五、后端开发指南

### 5.1 添加新的 API 路由

**在 `app.js` 中添加**:

```javascript
app.get('/your-api', async (req, res) => {
    const param = req.query.param;
    
    try {
        // 数据库查询
        const sql = 'SELECT * FROM your_table WHERE field = $1';
        const results = await db.any(sql, [param]);
        
        // 返回结果
        res.json({ status: 'success', data: results });
    } catch (err) {
        console.error(err);
        res.status(500).send('Internal Server Error');
    }
});
```

### 5.2 PostGIS 查询示例

**查询边界并返回 GeoJSON**:

```javascript
const sql = 'SELECT ST_AsGeoJSON(geom) as geojson_geom, * FROM "SCHEMA"."TABLE" WHERE code = $1';
const results = await db.any(sql, [code]);

// 构建 FeatureCollection
const geojsonFeatureCollection = {
    type: "FeatureCollection",
    features: results.map(result => {
        const geojsonGeom = JSON.parse(result.geojson_geom);
        delete result.geojson_geom;
        delete result.geom;
        return {
            type: "Feature",
            geometry: geojsonGeom,
            properties: result
        };
    })
};
```

### 5.3 矢量数据转换

**GeoJSON 转 Shapefile**:

```javascript
const shpwrite = require('./public/nodepack/shp-write/dist/index.js');

async function CovertShpFromGson(dataCode, res) {
    const gsonFilePath = path.join(__dirname, 'public', 'vectordata', `${dataCode}.gson`);
    const geojsonData = JSON.parse(fs.readFileSync(gsonFilePath, 'utf8'));
    
    const options = {
        folder: "请关注公众号锐多宝",
        outputType: "nodebuffer",
        compression: "DEFLATE"
    };
    
    const zipDataBuffer = await shpwrite.zip(geojsonData, options);
    fs.writeFileSync(`${dataCode}.zip`, zipDataBuffer);
    res.download(`${dataCode}.zip`);
}
```

**GeoJSON 转 SVG**:

```javascript
const { GeoJSON2SVG } = require('geojson2svg');
const geojsonExtent = require('geojson-extent');

async function CovertSVGFromGson(dataCode, res) {
    const geojsonData = JSON.parse(fs.readFileSync(`${dataCode}.gson`, 'utf8'));
    const extent = geojsonExtent(geojsonData);
    
    const converter = new GeoJSON2SVG({
        mapExtent: { left: extent[0], bottom: extent[1], right: extent[2], top: extent[3] },
        viewportSize: { width: 200, height: 100 },
        attributes: { stroke: 'blue', fill: 'none', 'stroke-width': '0.3' }
    });
    
    const svgStrings = converter.convert(geojsonData);
    // ... 保存 SVG
}
```

---

## 六、部署与运维

### 6.1 服务器环境

**本机部署路径**:

| 项目 | 路径 |
|------|------|
| 应用代码 | `/opt/1panel/apps/openresty/openresty/www/wwwroot/map.ruiduobao.com/` |
| SSL证书 | `/opt/1panel/apps/openresty/openresty/www/sites/map.ruiduobao.com/ssl/` |
| Nginx配置 | `/opt/1panel/apps/openresty/openresty/conf/conf.d/map.ruiduobao.com.conf` |
| 静态资源 | `public/vectordata/` (自动生成) |

**原服务器信息** (已迁移):
- IP: `182.254.147.254`
- 数据已迁移至本机

### 6.2 启动服务

```bash
cd /opt/1panel/apps/openresty/openresty/www/wwwroot/map.ruiduobao.com
node app.js
```

服务端口: **3003**

**进程管理**:

```bash
# 查看进程
ps aux | grep "node.*map.ruiduobao"

# 查看端口占用
lsof -i :3003

# 重启服务
kill $(lsof -t -i:3003)
cd /opt/1panel/apps/openresty/openresty/www/wwwroot/map.ruiduobao.com
nohup node app.js > /tmp/map.ruiduobao.log 2>&1 &
```

### 6.3 使用 PM2 守护进程

```bash
pm2 start app.js --name map-ruiduobao
pm2 save
```

### 6.4 Nginx 配置

网站通过 1Panel 的 OpenResty 反向代理到 Node.js 服务。

**配置文件位置**: `/opt/1panel/apps/openresty/openresty/conf/conf.d/map.ruiduobao.com.conf`

**关键配置**:

```nginx
server {
    listen 443 ssl http2;
    server_name map.ruiduobao.com;

    ssl_certificate /www/sites/map.ruiduobao.com/ssl/fullchain.pem;
    ssl_certificate_key /www/sites/map.ruiduobao.com/ssl/privkey.pem;

    location / {
        proxy_pass http://172.17.0.1:3003;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

### 6.5 定时任务

**已有定时任务** (通过 crontab 管理):

| 时间 | 任务 | 说明 |
|------|------|------|
| `*/5 * * * *` | PostgreSQL 检查 | 检查数据库是否启动，未启动则启动 |
| `0 2 * * *` | 清理缓存 | 执行 `scripts/cleanup_cache.sh`，清理 cache 目录 |
| `0 6 * * *` | 清理 PM2 日志 | 执行 `scripts/cleanup_logs_daily.sh` |
| `0 * * * *` | 磁盘空间监控 | 磁盘低于 1G 时自动清理 |
| `0 0 * * *` | PM2 重启 | 每天凌晨重启 Node.js 应用 |
| `0 3 * * *` | 系统重启 | 每天凌晨 3 点重启服务器 |

**缓存目录说明**:
- `public/vectordata/`: 矢量数据缓存（不清理）
- `cache/`: 预加载数据缓存
- `cache/provinces/`: 31省预生成缓存文件（不会被清理脚本删除）

**清理脚本位置**: `scripts/cleanup_cache.sh`

### 6.6 数据库迁移:

```bash
# 1. 在原服务器导出
sudo -u postgres pg_dump -Fc shengshixian > /tmp/shengshixian.dump

# 2. 传输到本机
scp root@182.254.147.254:/tmp/shengshixian.dump /tmp/

# 3. 导入数据库
sudo -u postgres pg_restore --dbname=shengshixian --clean --if-exists /tmp/shengshixian.dump
```

**数据库大小**: 约 3.6GB

---

## 七、高德地图 API 配置

### 7.1 API Key

项目中使用的高德地图 API Key:
- `a73eda1d713ad6a23f2712b7fe99161d` - 地理编码
- `b6ba147ffd1e49158d12f7cb16d0f381` - 地图显示 + 地理编码

### 7.2 引用方式

```html
<script src="https://webapi.amap.com/maps?v=1.4.15&key=YOUR_KEY"></script>
```

---

## 八、数据处理流程

### 8.1 行政区划数据处理

1. **CSV 分类**: `第1步_处理csv文件,归类为一到五级.py`
2. **生成 Markdown**: `第2步_生成省市县乡村带编码链接的markdown.py`
3. **生成 HTML**: `第3步_基于markmap批量生成html.ps1`
4. **添加交互**: `第4步_将带有链接的html文件更改属性.py`
5. **扩展级别**: `第5步_添加扩展级别_删掉标识.js`
6. **点击事件**: `第6步_添加内嵌的点击反映代码.js`

### 8.2 数据导入 PostGIS

```bash
shp2pgsql -s 4326 -I your_shapefile.shp schema.table | psql -d shengshixian
```

### 8.3 村级面数据导入

村级面数据存储在 `处理脚本/data/全国村/` 目录下，按市级代码分类存储。

**导入脚本**: `处理脚本/import_cun_polygon.py`

**数据结构**:
- 表名: `cunpolygon.{城市代码}` (如 `cunpolygon.140100`)
- 数据来源: 140个 shapefile 文件
- 属性字段: `name`, `SHI_code`, `CUN_CODE`, `orderid`

**导入流程**:

```bash
cd /opt/1panel/apps/openresty/openresty/www/wwwroot/map.ruiduobao.com/处理脚本
python3 import_cun_polygon.py
```

**坐标系转换**: shp2pgsql 参数 `-s 4490:4326` 将 CGCS2000 转换为 WGS84

---

## 九、反爬虫机制

### 9.1 机制说明

系统实现了三层反爬虫策略：

| 下载次数 | 处理方式 |
|----------|----------|
| < 20 次 | 正常下载 |
| 20-100 次 | 弹出密码验证框，需输入密码 `55555` |
| > 100 次 | 禁止下载 30 分钟 |

### 9.2 相关 API

#### 检查下载状态

```
GET /checkDownloadStatus
```

**响应示例**:
```json
{
    "count": 25,
    "needPassword": true,
    "isBanned": false,
    "banEndTime": null
}
```

#### 密码验证

```
POST /verifyPassword
Content-Type: application/json

{"password": "55555"}
```

**响应示例**:
```json
{
    "success": true,
    "message": "验证成功"
}
```

### 9.3 前端实现

**密码验证弹窗**:

```html
<div id="passwordModal">
    <!-- 显示微信公众号二维码 -->
    <img src="/pics/gongzhonghao.jpg" alt="微信公众号二维码">
    <!-- 密码输入框 -->
    <input type="password" id="passwordInput">
    <button onclick="submitPassword()">确认</button>
</div>
```

**下载流程**:

```javascript
async function downloadData(format) {
    // 1. 检查下载状态
    const status = await fetch('/checkDownloadStatus').then(r => r.json());

    // 2. 如果被封禁
    if (status.isBanned) {
        showBanModal();
        return;
    }

    // 3. 如果需要密码
    if (status.needPassword) {
        showPasswordModal();
        return;
    }

    // 4. 正常下载
    window.location.href = downloadUrl;
}
```

---

## 十一、外部链接

### 11.1 顶部导航图标

| 图标 | 链接 | 说明 |
|------|------|------|
| 使用说明 | `/others/使用说明.html` | 本地帮助文档 |
| GitHub | `https://github.com/ruiduobao/gaode_MAP_CUN` | 开源代码仓库 |
| 遥感论坛 | `https://www.rserforum.com/` | 遥感技术论坛 |

### 11.2 图标资源

| 文件 | 路径 | 用途 |
|------|------|------|
| `inst.png` | `/pics/inst.png` | 使用说明图标 |
| `github.svg` | `/pics/github.svg` | GitHub 图标 |
| `forum.svg` | `/pics/forum.svg` | 论坛图标 |
| `gongzhonghao.jpg` | `/pics/gongzhonghao.jpg` | 微信公众号二维码 |

---

## 十二、常见问题

### 9.1 地图不显示

**检查项**:
1. 高德地图 API Key 是否有效
2. 网络是否能访问高德服务器
3. 浏览器控制台是否有错误

### 9.2 矢量数据无法下载

**检查项**:
1. `public/vectordata/` 目录是否存在
2. 目录权限是否正确
3. 数据库连接是否正常

### 9.3 村级数据查询失败

**可能原因**:
1. 数据库中不存在该村数据
2. 地名格式不正确
3. 高德地理编码失败

### 9.4 左侧县市列表不显示

**问题现象**: 进入省份后，左侧 iframe 中没有显示下属县市

**原因分析**:
- 左侧县市数据通过 iframe 加载 `/year/{年份}/{省份}.html` 文件
- 这些 HTML 文件使用 **markmap** 库渲染思维导图
- 如果 HTML 文件内容为空或不完整，则无法显示数据

**检查方法**:
```bash
# 检查文件是否存在
ls -la /opt/1panel/apps/openresty/openresty/www/wwwroot/map.ruiduobao.com/year/2023年/

# 检查文件内容是否完整（正常应有完整的 markmap 数据）
head -50 /opt/1panel/apps/openresty/openresty/www/wwwroot/map.ruiduobao.com/year/2023年/山西省.html
```

**解决方案**:
1. 从原服务器迁移完整的 HTML 文件
2. 或重新运行数据处理脚本生成 HTML 文件

### 9.5 数据库连接失败 (500错误)

**问题现象**: API 返回 500 错误，日志显示 `relation "SHENG.CHN_sheng_2023" does not exist`

**原因**: 数据库 schema 或表未正确迁移

**解决方案**:
1. 检查数据库 schema 是否存在:
```sql
\dn  -- 列出所有 schema
```

2. 检查表是否存在:
```sql
\dt "SHENG".*
\dt "SHI".*
\dt "XIAN".*
```

3. 如果缺失，需要从备份恢复或重新导入数据

### 10.6 高德地图 API 警告

**问题现象**: 控制台出现 `document.write` 相关警告

**原因**: 高德地图旧版本 API 使用 `document.write` 加载模块

**解决方案**: 此为高德地图 API 的已知问题，不影响正常使用。可考虑升级 API 版本。

---

## 十三、未来扩展建议

### 13.1 功能扩展

- [x] 村级面数据导入 (已完成)
- [x] 必应卫星底图 (已完成)
- [x] 反爬虫机制 (已完成)
- [x] GitHub 开源链接 (已完成)
- [x] 论坛链接 (已完成)
- [x] 添加行政区划搜索功能 (已完成 v2.0)
- [x] 支持更多矢量格式 (KML, GeoPackage) (已完成 v2.0)
- [x] 添加数据可视化图表 (已完成 v2.0)
- [x] 支持用户上传自定义边界 (已完成 v2.0)
- [x] 多年份省市县数据支持 (已完成 v2.0)
- [x] 村级编码匹配 (已完成 v2.0)
- [ ] 村级多匹配选择UI
- [ ] Redis 缓存热点数据

### 13.2 性能优化

- [x] 实现矢量数据缓存机制 (已完成 v2.0)
- [x] 数据库查询优化 - 添加索引 (已完成 v2.0)
- [ ] 使用 Redis 缓存热点数据

### 13.3 安全加固

- [x] 反爬虫机制 (已完成)
- [ ] 数据库密码加密存储
- [ ] API Key 环境变量化
- [ ] 添加请求频率限制

---

## 十四、相关资源

- **GitHub 仓库**: https://github.com/ruiduobao/gaode_MAP_CUN
- **高德地图 API 文档**: https://lbs.amap.com/api/javascript-api/summary
- **PostGIS 文档**: https://postgis.net/documentation/
- **Express.js 文档**: https://expressjs.com/

---

## 十八、移动端开发指南 (v3.1新增)

### 18.1 移动端架构

**核心文件**:
- `/public/mobile-selector.html` - 手机端专用下拉选择器
- `/views/index.ejs` - 主页面，根据屏幕宽度自动切换
- `/public/css/styles.css` - 响应式样式

**切换逻辑**:
```javascript
function loadMobileSelector() {
    if (window.innerWidth <= 768) {
        iframe.src = '/mobile-selector.html';  // 手机端下拉
    } else {
        iframe.src = '/province-selector.html'; // PC端树状
    }
}
```

### 18.2 手机端界面设计

**默认状态** (地图隐藏):
```
┌─────────────────────┐
│ 📍 行政区划选择      │
├─────────────────────┤
│ 📅 年份 [2023年 ▼]  │
│ 🏛️ 省份 [四川省 ▼]  │
│ 🏙️ 地级市 [▼]       │
│ 🏘️ 县/区  [▼]       │
│ 🏚️ 乡镇   [▼]       │
│ 🏠 村    [▼]        │
├─────────────────────┤
│ [重置] [📍 查看地图] │
│ [📦 批量下载 (SHP)] │
└─────────────────────┘
      🗺️ (浮动按钮)
```

**显示地图后**:
```
┌─────────────────────┐
│ 下拉选择器 (50%)    │
├─────────────────────┤
│                     │
│    地图 (50%)       │
│                     │
└─────────────────────┘
      📋 (浮动按钮)
```

### 18.3 联动下拉选择

**数据流**:
```
选择省份 → 预加载该省所有数据 → 加载市级列表
    ↓
选择市级 → 加载县级列表
    ↓
选择县级 → 加载乡镇列表 + 显示批量下载按钮
    ↓
选择乡镇 → 加载村级列表
```

**预加载机制**:
```javascript
async function preloadProvinceData(provinceName) {
    const year = currentSelection.year || '2023';
    fetch(`/api/tree/preload?year=${year}&province=${encodeURIComponent(provinceName)}`)
        .then(res => res.json())
        .then(data => console.log('预加载完成:', data.cities, '个市'));
}
```

### 18.4 API缓存优化

**问题**: 原来每次选择都要读取整个CSV文件（约60万行），非常慢

**解决方案**: 
1. **API缓存**: 所有树状API添加24小时内存缓存
2. **预加载API**: 选择省份后后台预加载该省所有数据

**新增API**:
```
GET /api/tree/preload?year=2023&province=四川省
```

**效果**:
| 操作 | 优化前 | 优化后 |
|------|--------|--------|
| 首次选择省份 | 2-5秒 | 2-5秒（读取CSV） |
| 选择市级 | 2-5秒 | 秒开（缓存） |
| 选择县级 | 2-5秒 | 秒开（缓存） |
| 后续访问 | 2-5秒 | 全部秒开（缓存） |

### 18.5 响应式断点

| 断点 | 布局 |
|------|------|
| > 768px | PC端，左右分栏，树状视图 |
| ≤ 768px | 平板，上下布局 |
| ≤ 599px | 手机，全屏下拉选择器，默认隐藏地图 |

---

## 十九、重要技术认知更新 (v3.1)

### 19.1 下载功能关键点

**问题1**: 单独下载报错 "文件不存在"

**原因**: 下载时 `.gson` 文件可能还未生成

**解决**: `downloadVector` API 在文件不存在时自动从数据库生成

```javascript
// 判断是编码还是地名
if (/^\d+$/.test(id)) {
    // 是编码，调用 getGsonDataByCode(id, year)
    const results = await getGsonDataByCode(id, year);
} else {
    // 是地名，尝试地理编码
    const response = await axios.get(GAODE_GEOCODE_URL);
}
```

**问题2**: 批量下载报错 "column '地名' does not exist"

**原因**: 乡镇表字段名是 `name` 而不是 `"地名"`

**解决**: 修正SQL查询
```sql
-- 错误
SELECT ... "地名" as name FROM "XIANG"."CHN_xiang_2020"

-- 正确
SELECT ... name FROM "XIANG"."CHN_xiang_2020"
```

### 19.2 年份参数传递

**问题**: 下载时没有传递年份，后端固定使用2023年

**解决**: 
- 前端下载URL添加 `&year=${year}` 参数
- 后端 `getGsonDataByCode(dataCode, year)` 支持年份参数
- 批量下载也支持年份参数

### 19.3 地图切换按钮ID错误

**问题**: 地图切换不生效

**原因**: `switchMapLayer` 获取的是父div的ID而非select的ID

```javascript
// 错误
const selectedLayer = document.getElementById('mapLayer').value;

// 正确
const selectedLayer = document.getElementById('mapLayerSelect').value;
```

### 19.4 移动端默认隐藏地图

**设计理念**: 手机端用户主要需求是选择区域下载，地图是次要功能

**实现**:
```css
/* 手机端默认隐藏地图 */
@media (max-width: 599px) {
    .main-container #mapContainer {
        display: none !important;
    }
    .main-container.show-map #mapContainer {
        display: block !important;
    }
}
```

### 19.5 批量下载默认格式

**变更**: 批量下载默认格式从 GeoJSON 改为 **SHP**

**原因**: Shapefile 是GIS软件最常用的格式

```javascript
// 后端默认值
const format = req.query.format || 'shp';

// 前端下载链接
window.location.href = `/downloadCountyBatch/${code}?format=shp&year=${year}`;
```

---

## 二十、更新日志

| 日期 | 版本 | 更新内容 |
|------|------|----------|
| 2026-05-27 | 3.12 | **功能完善**: 1.乡镇级批量下载（新增 `/downloadTownBatch` 接口）；2.批量下载可选格式（SHP/GeoJSON/KML/GeoPackage）；3.村级数据无数据时提示用户；4.省份数据预缓存（31省缓存文件，秒级加载）；5.下载包新增站长微信二维码图片 |
| 2026-03-22 | 3.10 | **矢量样式调整**: 地图中的面矢量改为空心实线显示（`fillOpacity: 0`），只保留边界颜色，内部无填充透明 |
| 2026-03-19 | 3.9 | **手机端修复**: 1.下拉框弹窗改为90%屏幕高度，方便滑动选择；2.修复村级数据不显示问题（新增 getVillages 函数，优先使用预加载数据） |
| 2026-03-19 | 3.8 | **村级面数据禁用**: 默认禁用村级面数据（配置开关 `village.enablePolygon`），只提供村级点位数据；移除 ESRI 地形图底图；更新开发指南文档 |
| 2026-03-19 | 3.7 | **批量下载优化**: 添加使用声明（规范使用、自己负责、仅供参考、禁止商用、下载默认同意）、同级数据合并为单个文件（多面要素）、增加村级面数据和点数据下载、修复批量下载502错误(CSV文件不存在检查) |
| 2026-03-16 | 3.6 | **性能优化**: 数据库连接池配置、LRU缓存替代无限Map、SQL查询优化(ST_AsGeoJSON精度+乡镇查询合并)、Shapefile/GPKG异步生成、热点省份预加载、缓存定时清理、D3.js本地化 |
| 2026-03-14 | 3.5 | **Bug修复**: 修复乡镇级地名下载失败问题(增加后缀模糊匹配)、检查各级别下载功能 |
| 2026-03-14 | 3.4 | **性能优化**: 数据库空间索引、Redis缓存热点数据、Gzip压缩(压缩率96%+) |
| 2026-03-14 | 3.3 | **格式支持完善**: SHP使用GDAL生成真正的Shapefile、SVG手动实现矢量转换、完善SEO（结构化数据、移动端优化、预连接） |
| 2026-03-14 | 3.2 | **GeoPackage修复**: 使用GDAL ogr2ogr生成真正的GPKG文件；添加系统优化建议 |
| 2026-03-14 | 3.1 | **重大更新**: 修复下载功能(自动生成文件、年份参数支持)、手机端全新设计(下拉选择器、默认隐藏地图、预加载优化)、API缓存优化、下载说明添加免责声明、批量下载默认SHP格式 |
| 2026-03-14 | 3.0 | **Bug修复+移动端优化**: 修复单独下载报错(自动生成文件)、修复批量下载字段名错误、全新移动端体验 |
| 2026-03-13 | 2.9 | **移动端优化**: 手机端抽屉式面板、浮动菜单按钮、自动关闭面板、触摸优化(44px最小高度)、平板端响应式适配 |
| 2026-03-13 | 2.8 | **性能优化**: 懒加载机制(先显示市级)、预计算索引(0.05秒加载)、后台预加载完整数据 |
| 2026-03-13 | 2.7 | **文档完善**: 添加缓存机制详细说明、技术认知章节、常见问题排查指南 |
| 2026-03-13 | 2.6 | **下载修复+界面优化**: 修复下载ZIP功能、界面重新设计(更紧凑简洁)、下载包含二维码和说明文件 |
| 2026-03-13 | 2.5 | **性能优化**: 首页省份选择器静态化(31省+港澳台)、树状视图API内存缓存(24小时)、缓存过期时间改为3个月 |
| 2026-03-13 | 2.4 | **用户体验优化**: 修复复制功能、树状视图字体分级(省大村小)、下载ZIP包含公众号二维码、县级批量下载功能(县/乡镇/村)、下载面板位置优化、首页省份选择器改用树状视图 |
| 2026-03-13 | 2.3 | **功能完善**: 修复面数据表多命名格式支持(市级/省级/省份名称)、添加KML和GeoPackage下载格式、添加复制名称和编码功能、修复同乡镇不同村位置更新问题 |
| 2026-03-13 | 2.2 | **修复村级面数据查询**: 修正面数据表命名规则(市级编码6位)，优化匹配逻辑(精确→行政村→街道办整体)，修复村级面数据无法匹配的问题 |
| 2026-03-13 | 2.1 | **树状视图重构**: 使用API动态加载树状数据、支持乡镇级面矢量查询、村级编码精确匹配面数据、搜索功能优化(先搜本省再搜全国)、树状视图交互优化(整行可点击、hover阴影、选中状态)、修复图标下划线问题 |
| 2026-03-13 | 2.0 | **重大更新**: 多年份数据支持(2010/2017/2018/2021/2023)、村级编码匹配、行政区划搜索、KML/GeoPackage格式导出、数据可视化API、用户上传边界、数据库索引优化、矢量缓存机制 |
| 2026-03-13 | 1.2 | 添加必应底图、反爬虫机制、村级面数据导入说明、外部链接说明、论坛图标 |
| 2026-03-13 | 1.1 | 添加部署环境详情、数据库迁移说明、左侧县市列表实现原理、常见问题补充 |
| 2026-03-13 | 1.0 | 初始版本 |

---

## 十六、数据库优化说明 (v2.0+)

### 16.1 索引列表

以下索引已添加以优化查询性能：

```sql
-- 省级索引
CREATE INDEX idx_sheng_2010_code ON "SHENG"."CHN_sheng_2010" (first_gid);
CREATE INDEX idx_sheng_2017_code ON "SHENG"."CHN_sheng_2017" (first_gid);
CREATE INDEX idx_sheng_2018_code ON "SHENG"."CHN_sheng_2018" (first_gid);
CREATE INDEX idx_sheng_2021_code ON "SHENG"."CHN_sheng_2021" (first_gid);
CREATE INDEX idx_sheng_2023_code ON "SHENG"."CHN_sheng_2023" (first_gid);

-- 地级索引
CREATE INDEX idx_shi_2010_code ON "SHI"."CHN_shi_2010" (code);
CREATE INDEX idx_shi_2017_code ON "SHI"."CHN_shi_2017" (code);
-- ... 其他年份类似

-- 县级索引
CREATE INDEX idx_xian_2010_code ON "XIAN"."CHN_xian_2010" (code);
-- ... 其他年份类似

-- 村级索引
CREATE INDEX idx_cun_code ON "CUN"."chinacunpoint" (cuncode);
CREATE INDEX idx_cun_fullname ON "CUN"."chinacunpoint" (fullname);

-- 名称索引（用于搜索）
CREATE INDEX idx_shi_2023_name ON "SHI"."CHN_shi_2023" ("地名");
CREATE INDEX idx_xian_2023_name ON "XIAN"."CHN_xian_2023" ("地名");
```

### 16.2 缓存机制

系统实现了多层缓存机制：

#### 16.2.1 矢量数据缓存

- **缓存目录**: `public/vectordata/`
- **缓存过期**: 3个月 (90天)
- **清理频率**: 每2小时自动清理过期文件

```javascript
// 缓存配置
const CACHE_EXPIRY_MS = 90 * 24 * 60 * 60 * 1000; // 3个月
setInterval(clearCache, 2 * 60 * 60 * 1000); // 每2小时清理
```

#### 16.2.2 树状视图API缓存

- **缓存方式**: 内存缓存 (Map对象)
- **缓存有效期**: 24小时
- **缓存键**: `{year}_{province}_{level}`

```javascript
const treeCache = new Map();
const TREE_CACHE_EXPIRY = 24 * 60 * 60 * 1000; // 24小时
```

#### 16.2.3 预计算索引 (快速加载)

**重要**: 市级数据使用预计算索引，实现毫秒级加载：

```
cache/
├── shi_2010.json  # 2010年市级索引
├── shi_2017.json
├── shi_2018.json
├── shi_2021.json
└── shi_2023.json
```

**索引格式**:
```json
{
    "四川省": [
        {"name": "成都市", "code": "510100"},
        {"name": "绵阳市", "code": "510700"}
    ]
}
```

**加载时间对比**:
| 操作 | 优化前 | 优化后 |
|------|--------|--------|
| 市级加载 | 4.5秒 | 0.05秒 |

#### 16.2.4 懒加载机制

树状视图采用懒加载策略：

1. **首次加载**: 只加载市级数据（使用预计算索引，~50ms）
2. **展开市级**: 加载该市下的县级数据
3. **展开县级**: 加载该县下的乡镇级数据
4. **展开乡镇**: 显示村级数据
5. **后台预加载**: 首次加载后，后台自动加载完整数据

**API接口**:
```
GET /api/tree/full?year=2023&province=四川省&level=shi  # 只返回市级
GET /api/tree/full?year=2023&province=四川省&level=full # 返回完整数据
GET /api/tree/counties_lazy?year=2023&province=四川省&shi=成都市  # 懒加载县级
```

#### 16.2.4 首页省份选择器

**重要**: 首页省份选择器使用静态硬编码，无需API加载：

```html
<!-- 31个省份 + 港澳台，直接在HTML中定义 -->
<div class="province-grid">
    <div class="province-item" onclick="selectProvince('北京市', '110000')">北京市</div>
    <!-- ... 其他省份 -->
    <div class="province-item disabled">香港</div>  <!-- 不可点击 -->
    <div class="province-item disabled">澳门</div>
    <div class="province-item disabled">台湾省</div>
</div>
```

**优点**: 页面打开即显示，无需等待API响应。

---

## 十七、重要技术认知

### 17.1 村级数据字段差异（关键）

> ⚠️ **重要**: 村级面数据默认禁用，系统只使用 `CUN.chinacunpoint` 点位数据。配置项：`config.village.enablePolygon`

**问题**: 村级数据存储在两个表中，字段含义完全不同：

| 表 | 字段 | 实际存储 | 常见误解 | 当前状态 |
|-----|------|----------|----------|----------|
| `CUN.chinacunpoint` | `cuncode` | **县级编码(6位)** | 误认为是村级编码 | ✅ 启用 |
| `cunpolygon.{code}` | `cun_code` | **真正的村级编码(12位)** | - | ❌ 禁用 |

**解决方案**: 当前系统默认只使用点位数据。如需启用面数据，修改 `config/index.js`:
```javascript
village: {
    enablePolygon: true  // 启用村级面数据
}
```

### 17.2 面数据表命名不统一

**问题**: 村级面数据表命名有多种格式：

| 格式 | 示例 | 省份 |
|------|------|------|
| 市级编码 | `140100` | 太原市 |
| 省级编码 | `510000` | 四川省 |
| 省份名称 | `HENAN` | 河南省 |

**解决方案**: 查询时依次尝试市级编码、省级编码、省份名称。

### 17.3 乡镇名称后缀匹配（v3.5修复）

**问题**: 数据库中乡镇名称带有不同后缀，用户输入无法精确匹配：

| 数据库存储 | 用户输入 | 匹配结果 |
|------------|----------|----------|
| 东华门街道办事处 | 东华门街道 | ❌ 失败 |
| 某某镇 | 某某 | ❌ 失败 |

**解决方案**: `queryTownData` 函数增加了后缀智能匹配：

```javascript
// 匹配顺序
1. 精确匹配: name = '东华门街道'
2. 后缀匹配: name = '东华门街道' + '办事处' → '东华门街道办事处'
3. 模糊匹配: name LIKE '%东华门街道%'

// 支持的后缀
const suffixes = ['街道办事处', '街道', '镇', '乡', '民族乡', '苏木', '民族苏木'];
```

**直辖市特殊情况**: 北京、天津、上海、重庆没有市级数据，区县直接属于省级。

### 17.4 CSV数据与数据库编码差异

**问题**: CSV中的村级编码与数据库中的村级编码可能不匹配：

- CSV: `140105001001` (师范街社区居委会)
- 面数据表: `140105003205` (黑驼村)

**原因**: 城市社区可能没有单独的面数据，只有街道办整体边界（后缀999）。

**解决方案**: 精确匹配失败时，回退到乡镇前缀匹配或街道办整体边界。

### 17.5 性能优化要点

1. **首页静态化**: 省份列表硬编码，无需API
2. **API缓存**: 树状数据缓存24小时
3. **矢量缓存**: 3个月过期，减少重复查询
4. **前端缓存**: 下载文件添加时间戳参数避免浏览器缓存

### 17.6 常见问题排查

| 问题 | 可能原因 | 解决方案 |
|------|----------|----------|
| 村级无面数据 | 编码不匹配 | 检查 `cun_code` 字段 |
| 同乡镇不同村位置不变 | 浏览器缓存 | 添加时间戳参数 |
| 下载失败 | shp-write调用错误 | 使用 `zip()` 方法 |
| 树状加载慢 | 首次请求读CSV | 使用API缓存 |

---

## 二十一、性能优化建议

### 21.1 已完成的优化

| 优化项 | 状态 | 说明 |
|--------|------|------|
| API缓存 | ✅ | 树状API添加24小时内存缓存 |
| 预加载机制 | ✅ | 选择省份后后台预加载所有数据 |
| GeoPackage | ✅ | 使用GDAL ogr2ogr生成真正的GPKG文件 |
| 首页静态化 | ✅ | 省份列表硬编码，无需API |

### 21.2 已完成的优化

| 优化项 | 状态 | 说明 |
|--------|------|------|
| 数据库空间索引 | ✅ | 所有表添加 GIST 空间索引 |
| 数据库属性索引 | ✅ | 编码、名称字段添加索引 |
| Redis缓存 | ✅ | API 数据缓存 1 小时 |
| 内存缓存 | ✅ | 树状数据缓存 24 小时 |
| 预加载机制 | ✅ | 选择省份后后台预加载 |
| GeoPackage | ✅ | 使用GDAL生成真正的GPKG |
| Shapefile | ✅ | 使用GDAL生成完整的SHP文件集 |
| SVG | ✅ | 手动实现矢量转换 |
| Gzip压缩 | ✅ | 响应压缩率 90%+ |

#### Redis 缓存配置

```javascript
// Redis 配置
const REDIS_ENABLED = true;
const REDIS_TTL = 3600; // 1小时缓存

// 缓存策略：Redis → 内存 → 数据库
async function cacheGet(key) {
    if (!redisClient) return null;
    const data = await redisClient.get(key);
    return data ? JSON.parse(data) : null;
}
```

#### 数据库索引列表

```sql
-- 空间索引
CREATE INDEX idx_sheng_geom ON "SHENG"."CHN_sheng_2023" USING GIST(geom);
CREATE INDEX idx_shi_geom ON "SHI"."CHN_shi_2023" USING GIST(geom);
CREATE INDEX idx_xian_geom ON "XIAN"."CHN_xian_2023" USING GIST(geom);
CREATE INDEX idx_xiang_geom ON "XIANG"."CHN_xiang_2020" USING GIST(geom);
CREATE INDEX idx_cun_geom ON "CUN"."chinacunpoint" USING GIST(geom);

-- 属性索引
CREATE INDEX idx_xiang_code ON "XIANG"."CHN_xiang_2020" (code);
CREATE INDEX idx_cun_cuncode ON "CUN"."chinacunpoint" (cuncode);
```

#### Gzip 压缩效果

| 响应 | 原始大小 | 压缩后 | 压缩率 |
|------|----------|--------|--------|
| 城市列表 | 1351 bytes | 50 bytes | 96.3% |
| 年份列表 | 51 bytes | 46 bytes | 9.8% |
| 矢量数据 | ~100KB | ~10KB | 90%+ |

### 21.3 格式支持状态

| 格式 | 状态 | 说明 |
|------|------|------|
| GeoJSON | ✅ | 原生支持 |
| Shapefile | ✅ | 使用 GDAL ogr2ogr 生成，包含 .shp/.shx/.dbf/.prj |
| KML | ✅ | 自定义 KML 生成函数 |
| GeoPackage | ✅ | 使用 GDAL ogr2ogr 生成真正的 .gpkg 文件 |
| SVG | ✅ | 手动实现坐标转换生成矢量 SVG |

---

**文档维护**: 每次重大更新后，请同步更新本文档。

---

## 二十二、2026-03-16 性能优化记录 (v3.6)

### 22.1 优化概览

本次优化针对效率、加载速度、数据库三个维度进行了全面优化。

| 优化维度 | 优化项 | 预期效果 |
|----------|--------|----------|
| **数据库** | 连接池配置 | 高并发时更稳定，连接复用 |
| **数据库** | SQL 查询优化 | 减少 CPU 开销，查询更快 |
| **缓存** | LRU 缓存 | 防止内存溢出，自动淘汰冷数据 |
| **缓存** | 热点预加载 | 首次访问热门省份秒开 |
| **缓存** | 定时清理 | 自动清理过期文件，释放磁盘 |
| **异步** | Shapefile/GPKG 异步生成 | 不再阻塞其他请求 |
| **前端** | D3.js 本地化 | 减少外部网络依赖 |

### 22.2 详细优化说明

#### 22.2.1 数据库连接池配置

**问题**: 原配置使用默认连接池，高并发时可能成为瓶颈。

**优化**:
```javascript
const dbConfig = {
    host: 'localhost',
    port: 5432,
    database: 'shengshixian',
    user: 'ruiduobao',
    password: 'RDB123456.',
    // 新增连接池配置
    max: 20,                      // 最大连接数
    min: 2,                       // 最小连接数
    idleTimeoutMillis: 30000,     // 空闲连接超时
    connectionTimeoutMillis: 5000 // 连接超时
};
```

**预期效果**:
- 高并发时连接复用，减少连接创建开销
- 防止连接泄漏导致数据库资源耗尽

#### 22.2.2 LRU 缓存替代无限增长的 Map

**问题**: 原 `treeCache` 使用 `new Map()`，无上限，内存可能无限增长。

**优化**:
```javascript
const { LRUCache } = require('lru-cache');

const treeCache = new LRUCache({
    max: 500,                     // 最多缓存 500 个条目
    ttl: 24 * 60 * 60 * 1000,    // 24小时过期
    maxSize: 200 * 1024 * 1024,   // 最大 200MB
    sizeCalculation: (value) => {
        try {
            return JSON.stringify(value).length;
        } catch {
            return 1000;
        }
    }
});
```

**预期效果**:
- 内存占用上限 200MB，防止 OOM
- 自动淘汰最少使用的数据
- 热点数据常驻缓存

#### 22.2.3 SQL 查询优化

**优化1: ST_AsGeoJSON 精度控制**

```sql
-- 优化前：默认输出 15 位小数
SELECT ST_AsGeoJSON(geom) as geojson_geom FROM ...

-- 优化后：输出 6 位小数（约 0.1 米精度，足够 GIS 应用）
SELECT ST_AsGeoJSON(geom, 6) as geojson_geom FROM ...
```

**预期效果**: JSON 输出体积减少约 30%，数据库 CPU 开销降低。

**优化2: 乡镇地名查询合并**

```javascript
// 优化前：最多 8 次数据库查询
async function queryTownData(dataCode, placeName) {
    // 第1次：精确匹配
    let results = await db.any(sql, [placeName]);
    if (results.length > 0) return results;

    // 第2-8次：尝试各种后缀
    for (const suffix of suffixes) {
        results = await db.any(sql, [placeName + suffix]);
        if (results.length > 0) return results;
    }
    // ...
}

// 优化后：单次 SQL
async function queryTownData(dataCode, placeName) {
    const sql = `
        SELECT DISTINCT ON (code) ST_AsGeoJSON(geom, 6) as geojson_geom, *
        FROM "XIANG"."CHN_xiang_2020"
        WHERE name = $1
           OR name = $1 || '街道办事处'
           OR name = $1 || '街道'
           OR name = $1 || '镇'
           OR name = $1 || '乡'
           OR name = $1 || '民族乡'
           OR name = $1 || '苏木'
           OR name = $1 || '民族苏木'
           OR name LIKE '%' || $1 || '%'
        LIMIT 10
    `;
    return await db.any(sql, [placeName]);
}
```

**预期效果**: 乡镇查询从最多 8 次数据库调用 → 1 次，响应时间减少 70-90%。

#### 22.2.4 Shapefile/GPKG 异步生成

**问题**: 原 `execSync(ogr2ogr)` 同步执行，阻塞 Node.js 事件循环，导致其他请求等待。

**优化**:
```javascript
const { exec } = require('child_process');
const util = require('util');
const execAsync = util.promisify(exec);

// 优化前（同步，阻塞）
execSync(cmd, { timeout: 30000 });

// 优化后（异步，不阻塞）
await execAsync(cmd, { timeout: 30000 });
```

**预期效果**:
- Shapefile/GPKG 生成时不阻塞其他用户请求
- 服务器并发处理能力提升

#### 22.2.5 缓存清理定时任务

**优化**:
```javascript
// 启动时添加定时清理任务
setInterval(cleanOldCacheFiles, 24 * 60 * 60 * 1000);

function cleanOldCacheFiles() {
    const now = Date.now();
    const expiryMs = 90 * 24 * 60 * 60 * 1000; // 90天

    // 遍历缓存目录，删除过期文件
    const files = fs.readdirSync(CACHE_DIR);
    for (const file of files) {
        const stat = fs.statSync(filePath);
        if (now - stat.mtimeMs > expiryMs) {
            fs.unlinkSync(filePath);
        }
    }
}
```

**预期效果**: 自动清理过期缓存文件，防止磁盘空间不足。

#### 22.2.6 热点数据预加载

**优化**:
```javascript
const HOT_PROVINCES = [
    '四川省', '广东省', '山东省', '河南省', '江苏省',
    '浙江省', '湖北省', '湖南省', '河北省', '安徽省'
];

async function preloadHotProvinces() {
    for (const province of HOT_PROVINCES) {
        // 读取 CSV 并解析市级数据，存入缓存
        const result = await loadProvinceCities(province);
        treeCache.set(`cities_2023_${province}`, { data: result, time: Date.now() });
        await cacheSet(cacheKey, result, 3600); // Redis 缓存
    }
}
```

**预期效果**:
- 热门省份首次访问秒开（数据已在缓存）
- 日志示例: `[预加载] 四川省 完成，共 21 个市`

#### 22.2.7 前端资源优化

**优化**:
```html
<!-- D3.js 本地化，减少网络请求 -->
<script src="/js/d3.v7.min.js"></script>

<!-- 预连接优化 -->
<link rel="preconnect" href="https://webapi.amap.com">
<link rel="dns-prefetch" href="https://webapi.amap.com">
```

**预期效果**: D3.js 从本地加载，减少约 100ms 网络延迟。

### 22.3 优化效果对比

| 指标 | 优化前 | 优化后 | 提升 |
|------|--------|--------|------|
| 乡镇地名查询 | 最多 8 次 DB 调用 | 1 次 DB 调用 | **87.5% ↓** |
| 内存占用上限 | 无限制 | 200MB | 防止 OOM |
| 热门省份首次访问 | 2-5 秒 | 秒开 | **90%+ ↓** |
| Shapefile 生成 | 阻塞事件循环 | 异步执行 | 并发能力 ↑ |
| GeoJSON 输出体积 | 100% | 约 70% | **30% ↓** |
| 数据库连接 | 无池化 | 连接池复用 | 高并发稳定 |

### 22.4 依赖变更

```json
{
  "dependencies": {
    "lru-cache": "^11.0.0"  // 新增
  }
}
```

### 22.5 文件变更清单

| 文件 | 变更内容 |
|------|----------|
| `app.js` | 数据库连接池、LRU 缓存、SQL 优化、异步生成、缓存清理、热点预加载 |
| `views/index.ejs` | D3.js 本地化、预连接优化 |
| `public/js/d3.v7.min.js` | 新增本地 D3.js 文件 |
| `package.json` | 新增 lru-cache 依赖 |

### 22.6 后续优化建议

| 优先级 | 优化项 | 说明 |
|--------|--------|------|
| P0 | CSV 数据入库 | 将 CSV 文件数据导入数据库或预解析为 JSON，避免每次解析 |
| P0 | 添加数据库索引 | 为常用查询字段添加索引（如 code, name） |
| P1 | API 分页 | 搜索 API 添加分页限制，防止大结果集 |
| P2 | 日志轮转 | PM2 日志配置轮转，防止无限增长 |
| P2 | API Key 环境变量 | 敏感配置移至环境变量 |